{
  "description": "Nudges toward pstack:poteto-mode as the default entry point for non-trivial engineering tasks. Overridable by direct user instructions or CLAUDE.md/AGENTS.md, per the injected text itself. Set env var PSTACK_DISABLE_SESSION_HOOK=1 to disable this hook entirely.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$PSTACK_DISABLE_SESSION_HOOK\" = \"1\" ] || cat \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start-context.md\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
